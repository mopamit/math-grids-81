# המרחב המתמטי — math-grids-81

חבילה מלאה לפרסום ב־GitHub Pages.

## העלאה

יש להחליף בריפו את התיקיות `docs` ו־`source` בשלמותן בתיקיות שבחבילה.
בהגדרות GitHub Pages יש להשאיר את הענף `main` ואת התיקייה `/docs`.

## התיקון בגרסה זו

- נקודה מוצגת ברשימה כ־`A (1, 2)` בלי המילה "נקודה".
- קטע מוצג כ־`קטע AB` בלי אורך.
- זווית מוצגת כ־`זווית ABC` בלי ערך במעלות.
- עיגול ההצגה וההסתרה נשאר עגול בגודל קבוע גם כשהאובייקט מוסתר.
